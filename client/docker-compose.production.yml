version: "3.7"
services:
  mongodb:
    image: "mongo"
    volumes:
      - mongo-prod:/data/db
    env_file:
      - ./config/production.env
  frontend:
    build:
      context: ./client
      dockerfile: Dockerfile.production
    ports:
      - "90:90"
    env_file:
      - ./config/production.env
  backend:
    build:
      context: ./server
      dockerfile: Dockerfile.production
    ports:
      - "5000:5000"
    depends_on:
      - mongodb
    env_file:
      - ./config/production.env
volumes:
  mongo-prod: